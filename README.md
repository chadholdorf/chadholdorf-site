# Chad Holdorf Personal Site

This repository contains a simple, static personal website for **Chad Holdorf**. The design is lightweight, responsive and reflects a mid‑century + Northern‑California vibe.

## Files
- `index.html` – the main page with hero, about, focus, rides, AI, newsletter (placeholder) and contact sections.
- `styles.css` – minimal CSS that gives the site its clean look and mobile responsiveness.
- `CNAME` – tells GitHub Pages to serve the site at the custom domain **chadholdorf.com**.

## Publishing on GitHub Pages
1. **Create a GitHub repo** (e.g., `chadholdorf/chadholdorf-site`).
2. Clone the repo locally and copy the contents of `chadholdorf-site/` into the repository root.
   ```bash
   git clone git@github.com:chadholdorf/chadholdorf-site.git
   cd chadholdorf-site
   cp -R /Users/LC/.openclaw/workspace/chadholdorf-site/* .
   ```
3. **Commit & push**
   ```bash
   git add .
   git commit -m "Add personal site"
   git push origin main
   ```
4. **Enable GitHub Pages**
   - In the repo settings → Pages → Source, select the `main` branch (root folder).
   - Save. GitHub will publish the site at `https://chadholdorf.github.io/`.
5. **Set up the custom domain**
   - Add a DNS **CNAME** record pointing `www` (or the apex) to `chadholdorf.github.io`.
   - Ensure the `CNAME` file (already in the repo) contains exactly `chadholdorf.com`.
   - After DNS propagates (usually minutes), GitHub Pages will serve the site at `https://chadholdorf.com`.

## FAQ
- **Do I need a backend for the newsletter?** No – the form is a placeholder. It simply collects an email address locally and the submit button is disabled with the label *“Join Waitlist (Coming Soon)”*.
- **How to update the site?** Edit any of the source files, commit, and push. GitHub Pages rebuilds automatically.
- **Changing the domain?** Update the `CNAME` file and the DNS record, then push.

---
*Generated by the OpenClaw personal assistant.*
